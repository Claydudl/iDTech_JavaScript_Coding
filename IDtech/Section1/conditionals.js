let age = 10;

if (age >= 5 && age < 12) {
    // age, 5, 6, 7, 8, 9, 10, 11, 12
    console.log("you must be in elementry school!")
} else if (age >= 12 && age <18) {
    //age, 12, 13, 14
    console.log("You should be in middle school or highschool!")
} else (age <= 18 && age <5); {
    // age 18 ++, younger
    console.log("You aren't in school!")
} 
